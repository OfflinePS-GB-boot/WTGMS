server:
  port: 8180
  servlet:
    context-path: /locations-events

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8762/eureka}

spring:
  application:
    name: ms-locations-events
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://95.163.237.3:5432/wtg_db?currentSchema=locations_events_ms
    username: wtg
    password: wtg
  jpa:
    database: POSTGRESQL
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL82Dialect